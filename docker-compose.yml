services:
  eg4-monitor:
    image: localhost:5000/eg4-battery-monitor:latest
    container_name: eg4-battery-monitor
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command: ["eg4-monitor", "--config", "/app/config.yaml", "--no-ui"]
    network_mode: host
